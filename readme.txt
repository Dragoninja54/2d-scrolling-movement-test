Run game.java to start, "game" is fully in console
I appologize for the way the map prints to the console, when moving between IDEs I was lazy and just took out the method of clearing the console I was using so it's quite a bit more messy...
